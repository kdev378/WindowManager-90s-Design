# 要求 → 実装 → 検証の対応表

SPEC の各要求が、どのコードで実装され、どのテストで検証されているかの一覧。
**「仕様に書いてある」と「テストがある」は別物**なので、
そこを混同しないための表である。

凡例:

| 記号 | 意味 |
| --- | --- |
| **自動** | `make test` で毎回検証される |
| **半自動** | テストはあるが、通る条件が緩い（存在確認どまり等） |
| **手動** | `docs/MANUAL-TEST.md` の項目。実環境が要る |
| **未** | 検証手段が無い。壊れても気づけない |

---

## 1. 現状の全体像

| 層 | 実体 |
| --- | --- |
| 結合テスト | `tests/integration/` に 16 本。Xvfb + xprop / xwininfo / xdotool / wmctrl |
| 専用検証クライアント | C で 5 本（`100` sync/icon/ping、`120` XEmbed、`140` unmap、`150` ICCCM 異常系、`160` `_NET_WM_STATE`）。いずれもテストスクリプト内に埋め込み |
| 実アプリを使うもの | `130`（Java/Swing）。他は xterm |
| メモリ実測 | `make memcheck`（M0 / M1 / M20 / M20h）。SPEC §9.1 の 4 条件すべて実測済み |
| アトムの網羅 | `tools/check-supported.sh` が `_NET_SUPPORTED` の 56 アトム全部にテストの言及があることを機械的に検査 |
| 単体テスト | **無い**（`tests/unit/` は空。run-tests.sh は存在すれば拾う） |
| ASan / UBSan | **無い**（PLAN には書いてあるが Makefile ターゲットが無い） |
| 参照画像との比較 | `tools/pixdiff.sh` はあるが、**基準画像が無い**ので実質未稼働 |
| Xephyr 目視 | `tools/run-xephyr.sh` はあるが、開発環境に Xephyr が無く未使用 |

---

## 2. ICCCM（SPEC §5.1）

| 節 | 内容 | 実装 | 検証 | 状態 |
| --- | --- | --- | --- | --- |
| §4.1.2.3 | `WM_NORMAL_HINTS` の全フィールド | `icccm.c`, `hints_apply` | `150`（min>max / 0 / inc=0 / aspect 分母 0 / 未知 gravity） | 半自動 — **正常系の丸め結果を検算していない** |
| §4.1.2.4 | `WM_HINTS` | `icccm.c` | `150`（異常値）、`130`（input=False の実アプリ） | 自動 |
| §4.1.2.5 | `WM_CLASS` | `icccm.c` | **未** — タスクバーのグループ化に使う想定だがグループ化自体が未実装 | 未 |
| §4.1.2.9 | `WM_CLIENT_MACHINE` | `icccm.c` | **未** | 未 |
| §4.1.2.6 | `WM_TRANSIENT_FOR` | `icccm.c`, `stack.c` | `150`（自己参照 / ループ / 24 段の鎖） | 自動 — 異常系のみ。**正常な transient の積み上げ順は未検証** |
| §4.1.2.7 | `WM_PROTOCOLS` | `icccm.c` | `030`, `150`（プロトコル無しの窓を閉じる） | 自動 |
| §4.1.2.8 | `WM_COLORMAP_WINDOWS` | 認識のみ（v1.0 では何もしない） | 該当なし（`_NET_SUPPORTED` にも載せない） | 対象外 |
| §4.1.3.1 | `WM_STATE` | `client.c` | `140`（Normal / Iconic の遷移）、`150`（不正値の書き込み） | 自動 |
| §4.1.4 | 状態遷移と map/unmap | `event.c`, `client.c` | **`140`** — 4 系統を個別に | 自動 |
| §4.1.5 | ジオメトリと synthetic ConfigureNotify | `client.c` | **未** — 無視した `ConfigureRequest` に synthetic を返すことを検証していない。**10 種のグラビティも未検証** | 未 |
| §4.1.7 | 入力フォーカスの 4 モデル | `focus.c` | `130`（Globally Active を実アプリで端から端まで）、`050` | 自動 — ただし **No Input / Locally Active は未** |
| §4.3 | マネージャセレクション `WM_S<n>` | `wm.c` | **未** — `--replace` も他 WM への譲渡も検証していない | 未 |
| §2.8 | `MANAGER` クライアントメッセージ | `tray.c` | `120`（トレイのセレクション取得） | 自動 |

---

## 3. EWMH（SPEC §5.2）

### 3.1 ルートプロパティ

| アトム | 検証 | 状態 |
| --- | --- | --- |
| `_NET_SUPPORTED` / `_NET_SUPPORTING_WM_CHECK` | `010` | 自動 |
| `_NET_CLIENT_LIST` / `_LIST_STACKING` | `020`, `060`, `140` | 自動 |
| `_NET_NUMBER_OF_DESKTOPS` / `_CURRENT_DESKTOP` / `_DESKTOP_NAMES` / `_GEOMETRY` / `_VIEWPORT` | `060`, `090` | 半自動（値の形式と範囲のみ） |
| `_NET_WORKAREA` | `090`, `100`, `110` | 自動（strut を引いた値を検算） |
| `_NET_ACTIVE_WINDOW` | `050`, `130`, `160` | 自動 |
| `_NET_SHOWING_DESKTOP` | `090` | 半自動 |

### 3.2 クライアントメッセージ

| アトム | 検証 | 状態 |
| --- | --- | --- |
| `_NET_CLOSE_WINDOW` | `030` | 自動 |
| `_NET_WM_STATE` | **`160`**（add/remove/toggle / 同時指定 / 未知アトム / 軸別最大化 / 連打） | 自動 |
| `_NET_MOVERESIZE_WINDOW` | `090` | 半自動 — **移動後の座標を検算していない** |
| `_NET_WM_MOVERESIZE` | `090` | 半自動 — **キャンセル（`_NET_WM_MOVERESIZE_CANCEL`）は未** |
| `_NET_RESTACK_WINDOW` | `090` | 半自動 |
| `_NET_REQUEST_FRAME_EXTENTS` | `090` | 半自動 |

### 3.3 クライアントプロパティ

| アトム | 検証 | 状態 |
| --- | --- | --- |
| `_NET_WM_NAME` | `010` | 半自動 |
| `_NET_WM_DESKTOP` | `090` | 半自動 |
| `_NET_WM_STRUT` / `_STRUT_PARTIAL` | `100`, `110` | 自動 |
| `_NET_WM_ICON` | `100` | 半自動 — **4 MB 上限と差し替えレースの経路は未** |
| `_NET_WM_USER_TIME` | `100` | 半自動 — **スティール防止の判定そのものは未** |
| `_NET_FRAME_EXTENTS` | `090` | 半自動 |
| `_NET_WM_ALLOWED_ACTIONS` | `090` | 半自動 |
| `_NET_WM_SYNC_REQUEST` / `_COUNTER` | `100` | 半自動 — 下記 §6 参照 |
| `_NET_WM_PING` | `100`（無応答クライアント） | 自動 |
| `_NET_WM_WINDOW_TYPE` (13 種) | **`170`** — 14 種すべてについて装飾・キャプションの大小・レイヤ・フォーカス可否を SPEC §5.2.2 の表と突き合わせ | 自動 |
| `_NET_WM_STATE_*` (12 種) | `160` | 自動 |

---

## 4. 埋まっていない穴（優先度順）

外部レビューの指摘と突き合わせた結果。**上ほど「壊れたときに気づけない度」が高い**。

| # | 内容 | なぜ危険か | 現状 |
| --- | --- | --- | --- |
| ~~1~~ | ~~ウィンドウ種別 13 種の挙動~~ | — | **済**（`170`。期待値を 4 通り壊して落ちることも確認済み） |
| 2 | **`_NET_WM_SYNC_REQUEST` の状態機械** | SPEC §7.3 は IDLE/WAITING/STALLED と 250ms タイムアウトを細かく定めている。`100` は「カウンタを作って `_NET_SUPPORTED` に載っている」までしか見ていない。**250ms が本当に 250ms 前後で解けるか**、カウンタを一気に進めた場合、古い alarm、カウンタ破棄、64bit の折り返し、が未検証 | **未** |
| 3 | **CSD 専用クライアント** | SPEC §7.2 は `_GTK_FRAME_EXTENTS` の読み直し、visible rect 基準の最大化、`_NET_WM_MOVERESIZE` のキャンセル、`_GTK_SHOW_WINDOW_MENU` まで定めている。GTK4 アプリを起動するだけでは全経路を踏めない | **未**（手動のみ） |
| 4 | **マルチモニタと strut の組み合わせ** | 2 画面・異解像度・上下配置・**負座標**・片方だけ strut・部分的に重なる strut。X11 の WM では負座標が特に事故りやすい。Xvfb は RandR の変更通知を配送しないので、**この環境では原理的に検証できない** | **未**（VM で手動） |
| 5 | **X 接続の切れ方** | 正常終了 / SIGTERM / SIGKILL / 接続断で、frame・タスクバーボタン・transient・sync alarm・MRU がすべて掃除されるか。`040` は WM 側を kill する話で、クライアント側の異常終了は未 | **未** |
| 6 | **X サーバ側リソースリーク** | 窓の生成/破棄を 10,000 回して Window / Pixmap / GC / **XSync Alarm** が元に戻るか。SPEC §7.3 が alarm のリークを明示的に警告している。メモリ（クライアント側）は M20h で見ているが、サーバ側は見ていない | **未** |
| 7 | **WM の終了・再起動・`--replace`** | ICCCM §4.3 のマネージャセレクション。`--replace` で既存 WM から切り替える経路は一度も走らせていない | **未** |
| 8 | **ICCCM §4.1.5 のグラビティ 10 種** | `icccm_apply_gravity` は 10 種を扱うが、検証は 1 種も無い | **未** |
| 9 | **タスクバーの極端な入力** | タイトルが極端に長い / 空 / アイコン無し / 巨大アイコン / アイコン変更連打 / 50 窓 | **未** |
| 10 | **単体テスト（幾何計算）** | `hints_apply`、`rect_overlap_area`、strut の適用規則は純粋関数なので単体で叩けるのに、`tests/unit/` が空 | **未** |
| 11 | **ASan / UBSan / 静的解析** | PLAN には書いてあるが Makefile ターゲットが無い。`150` の異常入力と組み合わせると効果が大きい | **未** |
| 12 | **参照画像との比較** | `tools/pixdiff.sh` はあるが基準画像が無い。`070` は座標を指定した画素値の直接照合で代替している | 半自動 |

---

## 5. 手動でしか確認できないもの

`docs/MANUAL-TEST.md` を参照。自動化できない理由が明確なものだけを残している。

| 項目 | 自動化できない理由 |
| --- | --- |
| 実アプリの描画（GTK / Qt / Electron / LibreOffice） | 開発環境にパッケージが無い。挙動がツールキットの版に依存する |
| トレイアイコンの**絵**が正しく出るか | アプリ側の描き方に依存する。XEmbed の配線自体は `120` で検証済み |
| RandR の再構成 | **Xvfb が RandR の変更通知を一切配送しない**（4 種すべて select したプローブで 1 件も届かないことを確認済み） |
| 日本語タイトルの見え方 | 環境の X コアフォント次第（core ビルドは best-effort） |
| Win98 実物との意匠比較 | 参照画像が要る |
| OpenBSD | Phase 5 |

---

## 6. この表の使い方

新しい要求を SPEC に足したら、**この表に行を足してから**実装する。
行が「未」のまま残るのは構わない（実際 12 個ある）が、
**表に存在しない要求を実装してはいけない**。
「仕様に書いたから実装したはず」を防ぐのがこの表の役目である。
